#!/bin/sh
cd {{ base_path }}/
. ~/.profile

export TXT_HOSTNAME=`hostname`
export TXT_MESSAGE="[$TXT_HOSTNAME] $1"

curl -H "Content-Type: application/json" -d "{ \"username\": \"PiBackup Bot\", \"content\": \"$TXT_MESSAGE\" }" {{ discord_webhook_url }}
